using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScriptDificuldades : MonoBehaviour
{
    public GameObject facil, medio, dificil;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*if(facil.activeSelf==false) {
            medio.SetActive(true);
            dificil.SetActive(true);
        }
        if(medio.activeSelf==false){
            facil.SetActive(true);
            dificil.SetActive(true);
        }
        if(dificil.activeSelf==false) {
            facil.SetActive(true);
            medio.SetActive(true);
        }*/
    }
}
